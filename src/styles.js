const styles = {
  btn: "flex w-[2rem] h-[2rem] bg-yellow items-center justify-center",
};

export const layout = {
  section: `flex md:flex-row flex-col ${styles.paddingY}`,
  sectionReverse: `flex md:flex-row flex-col-reverse ${styles.paddingY}`,
};

export default styles;
